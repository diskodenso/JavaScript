"use strict";

/*
const meineFunktion = function (parameter_1, parameter_2) {
    auszuführender Code
};

meineFunktion(x, "Superman");

> - - - - - Beispiel - - - - - <

let samba ole = "Max Mustermann ist ein";

const myFunction = function (parameter_1, parameter_2) {
    ausgeführter Code;
};
myFunction(samba ole, "Shitface");

Die Benennung der Parameter in der Function (Parameter_1 und 2)
hat nicht mit der Benennung der Variablen sowie der Variablen im 
Funktionsaufruf zu tun! 




> - - - - - MINI CHALLENGE - - - - - - - >

Schreibe eine Funktion 
- die zwei Parameter überbegeben bekommt
- Es soll in der Funktion 5 CLGs geben
- Es soll 5 Variablen geben
*/
let a = 12; 
let b = 15;
let c = 2; 
let d = 27;
let e = 98;

const miniChallenge = function (a, b) {
    console.log(parameter1 + parameter2);
    console.log(parameter1 * parameter2);
    console.log(parameter1 - parameter2);
    console.log(parameter1 / parameter2);
    console.log(parameter1 % parameter2);
};

miniChallenge(a, c);
miniChallenge(a, e);
miniChallenge(d, c);
miniChallenge(b, d);
miniChallenge(e, c);


// const logDieZahl = function (a) {
//     console.log(a);
// };
// logDieZahl(42);


// let vor = "Max";
// let nach = "Mustermann";

// const begruessung = function (vorname, nachname) {
//     console.log(`Hallo ${vorname} ${nachname}!`);
// };
// begruessung(vor, nach);